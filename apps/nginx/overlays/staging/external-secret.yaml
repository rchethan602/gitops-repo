# apps/nginx/overlays/staging/external-secret.yaml
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nginx-secret
  namespace: staging
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: SecretStore
  target:
    name: nginx-secret        # K8s secret name ArgoCD will create
    creationPolicy: Owner
  data:
  - secretKey: API_KEY        # key in K8s secret
    remoteRef:
      key: gitops/staging/nginx-secret    # AWS secret name
      property: API_KEY               # field in JSON secret
  - secretKey: DB_PASSWORD
    remoteRef:
      key: gitops/staging/nginx-secret
      property: DB_PASSWORD