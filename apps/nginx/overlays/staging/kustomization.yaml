apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: staging
resources:
- ../../base
- external-secret.yaml
images:
- name: public.ecr.aws/nginx/nginx
  newTag: "1.26.0"        # staging pinned to promoted version
patches:
- path: replica-patch.yaml