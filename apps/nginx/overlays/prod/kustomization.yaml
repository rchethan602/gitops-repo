apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: prod
resources:
- ../../base
images:
- name: nginx
  newTag: "1.25.0"        # prod pinned to last stable
patches:
- path: replica-patch.yaml